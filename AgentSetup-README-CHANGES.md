# 변경 이력

최신 항목이 위에 옵니다. 상세 사용법은 `AgentSetup-README.md`를 참조하세요.

## 상세 패널과 실시간 진행 바 (2026-08-02, 1.5.0)

**목록 한 줄에 우겨넣던 정보를 상세 패널로 내리고, 적용이 얼마나 남았는지
보이게 한다.** 80칸 터미널에서 힌트 한 줄에 상태·CLI 커버리지·미배선 사유까지
이어 붙이면 뒤쪽이 통째로 잘렸다. 적용 중에는 화면이 몇 분이고 멈춘 것처럼
보였다 — 실제로는 외부 명령이 순서대로 돌고 있었지만 알 길이 없었다.

- **목록 아래 상세 패널.** 커서 항목의 전문을 여러 줄로 편다 — 배선된
  CLI(MCP는 설정 파일 경로까지), 안 된 CLI와 사유(같은 사유는 하나로 묶는다),
  note·설명까지. 힌트 줄에는 상태와 `CLI n/10` 커버리지만 남긴다. 패널 높이는
  커서가 아니라 **터미널 크기로만** 정해진다 — 커서를 옮길 때마다 높이가
  바뀌면 목록이 출렁이기 때문이다. 지면이 모자라면 패널은 통째로 빠진다.
  `Ctrl+D`로 전체 화면까지 펼친다.
- **CLI 필터로 양방향 확인.** `Ctrl+F`/`Ctrl+B`가 CLI를 순환하며 그 CLI에
  배선되지 않는 항목을 목록에서 통째로 뺀다. 상세 패널이 "이 항목이 어디에
  배선되나"를 보여 준다면, 필터는 반대 방향에서 "이 CLI에서 뭐가 되나"에
  답한다. 글자 키(`c`·`d`)를 쓰지 않은 것은 의도적이다 — 목록 포커스에서
  아무 글자나 누르면 검색칸으로 올라가므로, `c`를 필터에 배정하면 `codex`를
  검색어로 칠 수 없게 된다.
- **외부 명령 실행을 비동기로 바꿔 진행 바를 그린다.** 적용 중에도 alt 화면을
  유지한 채 100ms마다 다시 그린다 — 지금 실행 중인 항목·명령·경과 시간·
  `완료/전체` 개수가 실시간으로 갱신된다. 명령 실행이 동기였을 때는 이벤트
  루프가 막혀 화면을 다시 그릴 수 없었다.
- **`Ctrl+C`는 항목 경계에서만 멈춘다.** 중단은 요청일 뿐이다 — 지금 실행
  중인 항목은 끝까지 마친 뒤에야 멈춘다. 패키지 설치나 파일 쓰기를 명령
  중간에 죽이면 반쯤 설치된 상태가 남기 때문이다. 아직 시작하지 않은 항목은
  "건너뜀"으로 표시된다.
- **비대화형과 CI는 평문으로.** `--set`과 파이프 입력에서는 바 대신 항목마다
  한 줄씩 진행을 흘린다 — ANSI 제어 문자로 로그를 더럽히지 않기 위해서다.
  부트스트랩은 동기 파일 작업이라 바 대신 로그 줄마다 `[n/총계]` 단계 번호를
  붙인다.

`1.5.0`은 136 파일로 발행된다.

## 동봉 번들의 상류 고지 (2026-08-01, 1.4.0)

**MIT를 지키는 데 빠져 있던 한 조각을 채운다.** 오프라인 설치를 위해 담는
DESIGN.md 74개는 [awesome-design-md](https://github.com/VoltAgent/awesome-design-md)의
사본이고 상류는 MIT다. MIT는 재배포를 허용하되 **사본에** 저작권 고지와 허가
문구를 함께 담을 것을 요구하는데, 지금까지 패키지에는 README 본문의 출처 링크
한 줄뿐이었다. 74개 전량은 substantial portion이 명백하므로 고지가 필요하다.

- **`cache/awesome-design-md/LICENSE.md`.** 상류 `LICENSE` 원문을 그대로 담고,
  위에 출처와 "원문을 고치지 않는다"를 적었다. 파일 이름이 `LICENSE`가 아닌
  이유는 발행물 검사가 최상위 `LICENSE` 하나만 확장자 없는 파일로 허용하기
  때문이다 — 요건을 채우는 것은 이름이 아니라 담긴 내용이다.
- **pack 검사가 지킨다.** 파일이 tarball에 실리는지, 그리고 저작권 고지·허가
  문구·출처 링크가 실제로 들어 있는지를 함께 본다. 링크 한 줄이나 빈 파일로는
  통과하지 못한다 — 조용히 빠지면 발행 자체가 조건을 어기게 된다.
- **번들 재생성이 고지를 흘리지 않게 한다.** `refresh-bundle`이 상류 `LICENSE`를
  받아 우리 고지와 대조하고, 어긋나면 알린다. 자동으로 덮어쓰지는 않는다 —
  고지에는 우리가 쓴 출처 설명이 함께 있어 원문으로 갈아치우면 맥락이 사라지고,
  라이선스가 바뀌는 것은 사람이 보고 판단할 일이다.

파일 자체에는 로고·폰트 같은 자산이 없다. 색상 hex와 산문 분석이며, 본문이
오히려 "이 서체는 독점이니 오픈소스로 대체하라"고 적고 있다.

`1.4.0`은 134 파일로 발행된다.

## 항목 6종 추가와 성격별 그룹핑 (2026-08-01, 1.3.0)

**고를 때 보이는 분류를 "무엇으로 설치되는가"에서 "무엇을 해 주는가"로 바꿨다.**
탭은 PLUGIN·MCP·SKILL, 즉 설치 기구다. 그런데 같은 탭 안에 토큰을 아끼는 것과
디자인을 봐 주는 것이 나란히 있어서, 고를 때 비교가 되지 않았다. 탭 안쪽에
성격 헤더를 붙인다 — **토큰 절감 · 코드 이해 · 디자인 감각 · 작업 방식 ·
외부 서비스** 순이다. design.md가 이미 쓰던 그룹 기구를 그대로 얹었으므로
렌더러는 손대지 않았다.

### 미지원 CLI를 고르기 **전에** 보이게

지금까지 미지원 사유는 **MCP 항목에서만** 나왔다. 그래서 CLI 두 개에만
들어가는 플러그인(`plugin.ponytail`)이 나머지 여덟 개를 화면에 한 글자도 알리지
않았다 — 고를 때 알 방법이 없었고, 적용한 뒤에야 드러났다.

- **`CLI n/10`을 상태 바로 뒤에 둔다.** 힌트 끝은 좁은 터미널에서 잘려 나가는데,
  하필 그때가 "이 항목이 내가 쓰는 CLI에서 되나"를 가장 알고 싶은 순간이다.
  **전부 지원할 때도 찍는다** — 표시가 없는 것과 `10/10`은 뜻이 다르다. 없으면
  "빠뜨린 것"과 "전부 되는 것"을 화면에서 가를 수 없다.
- **사유는 카테고리와 무관하게 나온다.** 같은 사유의 CLI는 하나로 묶어
  `미배선 9곳: codex·gemini·… — Claude Code 전용 플러그인`처럼 한 번만 적는다.
  사유가 둘 이상이면 갈래가 그대로 보인다. 아홉 번 반복하면 줄만 길어지고
  "무엇이 왜 빠졌는가"는 오히려 묻힌다.
- **제출 직전 검토 화면에도 붙인다.** 되돌릴 수 있는 마지막 지점이다. 단
  전부 지원하는 항목은 조용히 둔다 — 경고가 흔해지면 아무도 읽지 않는다.

### 스킬 레지스트리 경로 — 한 번 설치로 10개 CLI

`npx skills add`(vercel-labs/skills)의 `--agent universal` 프로젝트 경로가
**`.agents/skills`**다. 이 저장소가 이미 공유 스킬 자리로 쓰는 그 디렉터리다.
Claude·Kiro·Grok은 부트스트랩이 만든 Junction으로, 나머지는 그 경로를
네이티브로 읽으므로 **한 번 설치하면 10개가 함께 본다.** `--copy`로 실물을
남긴다 — 커밋해서 팀과 나누는 자리라 링크는 클론 뒤 깨진다.

- **`skill.caveman`** — 원시인 말투로 출력 토큰을 줄인다. 코드·오류는 그대로.
- **`skill.taste`** — 안티슬롭 프런트엔드 스킬. 저장소 폴더 이름은
  `taste-skill`인데 `SKILL.md`의 name은 `design-taste-frontend`다. 레지스트리는
  name으로 고르므로 **디렉터리 이름만 보고 판정하면 설치해 놓고도 계속
  '미설치'로 읽힌다** — frontmatter까지 읽어 찾는다.
- **`skill.karpathy`** — 과설계와 모호한 완료 기준을 줄이는 행동 지침.

레지스트리의 `remove`는 `--agent universal`에서 **"Done!"을 찍고도 디렉터리를
남긴다**(실측). 그래서 제거는 명령 결과와 무관하게 남은 디렉터리를 우리가
지운다. 폴백이 없으면 제거가 성공으로 보이고 다음 스캔에서 되살아난 것처럼
읽힌다.

### 플러그인으로 넣은 3종과 그 이유

- **`plugin.ecc`** — ECC의 `skills/`에는 281개가 들어 있다. `--skill` 없이
  레지스트리로 넣으면 공유 스킬 디렉터리를 통째로 덮는다. 프로필 관리는
  상류에 맡기고 플러그인으로 설치한다.
- **`plugin.impeccable`** — 상류의 멀티 CLI 경로
  (`npx impeccable install --providers=… --scope=project`)는 **쓰지 않는다.**
  실측 결과 그 명령이 `.claude/skills`를 실제 디렉터리로 갈아치워 부트스트랩이
  만든 `.agents/skills` Junction을 끊는다 — 공유 스킬이 전부 Claude Code
  화면에서 사라진다. 게다가 `--providers`와 `--scope`를 함께 주지 않으면
  대화형으로 묻고, 비TTY에서는 조용히 **global**을 골라 홈에 설치한다.
- **`plugin.understand-anything`** — 다른 CLI용 `install.sh`는
  `~/.understand-anything`에 클론하고 홈에 링크를 건다. 사용자 스코프라
  이 저장소의 항목이 될 수 없다.

`caveman-code`는 항목으로 넣지 않았다. 스킬이나 설정이 아니라 `npm install -g`로
설치하는 **독립 터미널 에이전트**다 — 저장소 안에 배선할 대상이 없다.

### Node 20에서 테스트가 한 개도 돌지 않고 있었다

발행 워크플로를 처음 돌려 보고 드러났다. `node --test "test/*.test.mjs"`의
**글로브 확장은 Node 21+ 기능**이라, 하한으로 선언한 20에서는 리터럴 경로로
취급돼 `Could not find`로 끝난다. 테스트가 실패한 게 아니라 **하나도 실행되지
않았다.** 관례 탐색(`node --test`)으로 바꾼다 — 18부터 모든 판에서 같게 돈다.

그렇게 20에서 처음 돌려 보니 의존성 통합 테스트가 전제부터 무너져 있었다.
**Windows의 Node 20 `cpSync`는 filter에 확장 길이 경로(`\\?\D:\…`)를 넘긴다.**
`relative()`가 `PKG_ROOT`와 관계를 세우지 못해 절대 경로가 돌아오고, 첫 구간이
`\\?\D:`가 되어 어떤 제외도 걸리지 않는다. `node_modules`가 통째로 복사되어
"의존성이 없는 트리"가 아니게 되고, 시험 대상이 사라진 채 통과했다. 접두사를
떼고 비교하며, **전제 자체를 단언으로 못 박았다** — 제외가 다시 깨지면 그
자리에서 실패한다.

세 번째는 리눅스에서 드러났다. `parseDirSpec` 검사가 `D:\a=b\c`를 쓰고 있었는데,
역슬래시는 **Windows에서만** 구분자다. POSIX에서 그 문자열은 구분자가 하나도 없는
파일 이름이라 `basename`이 통째로 돌아오는 것이 정상인데, 테스트가 그것을 실패로
읽었다. 검증 대상은 "id 자리에 구분자가 있으면 id로 보지 않는다"이므로 슬래시
경로로 시험하고, 역슬래시 사례만 Windows에서 따로 본다. 코드는 두 판 모두에서
처음부터 옳았다 — 판을 가린 것은 테스트였다.

`1.3.0`은 133 파일로 발행된다.

## 토큰 절감 도구 3종을 저장소 범위로 (2026-08-01, 1.3.0)

**같은 도구를 사용자 스코프가 아니라 저장소 범위로, 그리고 Claude Code
하나가 아니라 붙일 수 있는 CLI 전부에 배선한다.** Ponytail·Graphify·Headroom은
토큰이 새는 지점이 서로 다르다 — 차례로 모델이 **쓰는** 양, 찾느라 **읽는** 양,
실제로 **보내는** 양이다. 상류의 설치 안내는 셋 다 `--scope user`이거나
`~/.claude/skills`이고 Claude Code만 본다. 그러면 옆자리 동료의 세션은 그대로
샌다.

- **`mcp.graphify`·`mcp.headroom` — 10개 CLI 프로젝트 설정에 동시 등록.**
  기존 MCP 항목과 같은 경로를 그대로 탄다. `headroom`은 상류가 배포하는
  `server.json`과 같은 계약(`headroom mcp serve`)으로 넣고, `graphify-mcp`는
  인자 없이 실행 디렉터리의 `graphify-out/graph.json`을 읽으므로 그래프까지
  저장소 안에 남는다.
- **`plugin.ponytail` — Claude Code와 OpenCode 두 곳.** Claude Code는 기존
  플러그인 항목과 같이 `--scope project`로 설치하고 실패하면
  `.claude/settings.json`에 기록한다. OpenCode는 `opencode.jsonc`의 `plugin`
  배열에 항목 하나를 **덧붙인다** — 이 저장소에서 배열을 건드리는 코드는
  여기뿐이라, 기존 항목 보존·멱등·`plugin`이 배열이 아닐 때 거부를 검사로
  못 박았다. 통째로 덮어쓰면 사용자가 적어 둔 설정이 조용히 사라진다.
- **바이너리는 설치하지 않는다.** 두 MCP 항목은 `graphify-mcp`·`headroom`이
  PATH에 있다고 가정하고, 없으면 항목 note가 `uv tool install` 명령을 보여
  준다. `mcp.codebase-memory`와 같은 규칙이다 — 설치기는 배선만 한다.
- **프록시(`headroom wrap claude`)는 항목이 아니다.** 저장소 설정에 담을 것이
  없는 실행 방식이라 note로만 안내한다.

나머지 8개 CLI에는 Ponytail을 배선하지 않는다. Codex·Copilot·Gemini는 상류
설치 경로가 있지만 전부 사용자 스코프이고, 나머지는 플러그인 기구 자체가 없어
규칙을 `AGENTS.md`에 옮겨 적어야 한다. 둘 다 항목의 미지원 사유로 화면에
적어 둔다 — 조용히 빠지는 것보다 이유가 보이는 편이 낫다.

## 다국어 지원 (2026-07-31, 1.2.0)

**한국어를 모르면 첫 화면에서 막히던 것을 고쳤다.** npm에 발행했지만 사용자에게
보이는 문자열 약 289곳이 전부 한국어였다. `npx @rch4com/agent-setup --help`의
첫 줄을 읽을 수 없으면 그 뒤는 없다.

- **영어가 기준 언어다.** 모든 키의 원본이 영어 카탈로그이고 한국어가 두
  번째다. 이번 실행의 언어는 `--lang <en|ko>` → `AGENT_SETUP_LANG` → 설치
  기록의 `lang` → OS 감지 → `en` 순으로 정해지며 앞이 이긴다. 지원하지 않는
  값은 조용히 건너뛰지만 `--lang`으로 **명시한** 값만은 거부한다 — 사용자가
  적어 넣은 것이 조용히 무시되면 안 된다.
- **번역 범위는 사용자에게 보이는 전부.** `--help` 5종, TUI, 부트스트랩·설치
  로그, `status`·`update` 출력, 오류 메시지.
- **전역 싱글턴을 두지 않는다.** 로케일을 확정한 뒤 `t`를 만들어 이미 있던 옵션
  주머니에 얹어 내린다. 그래야 순수 리듀서(`state.mjs`)가 계속 아무것도 import
  하지 않고, 테스트가 파일 병렬로 돌아도 로케일이 서로를 오염시키지 않는다.
  **새 의존성은 0개**다 — `lib/i18n/`은 아무것도 import 하지 않는 잎 모듈이라
  `bootstrap.isolation.test.mjs`가 이 불변식을 그대로 지킨다.
- **언어는 대화형 화면 첫 행에서 Enter로 순환한다.** 팝업을 따로 두지 않았다.
  읽을 수 없는 언어에 갇혀도 Enter만 반복하면 빠져나온다. 고른 값은 즉시
  `.agent-kit/agent-setup.json`의 `lang`에 저장되어 다음 실행에 이어지고,
  기록이 없으면 새로 만든다. 쓰기가 실패해도 화면을 죽이지 않는다 — 언어는
  부수적 설정인데 여기서 죽으면 설치기 자체를 못 쓴다.
- **기록 형식 버전은 1을 유지한다.** `lang`은 선택 필드 추가라 구버전 도구가
  읽어도 무시할 뿐이다. 올렸다면 기존 기록을 가진 저장소가 전부 막혔다.
- **옵션 주머니가 닿지 않는 곳은 `LocalizedError`.** 저장소 탐지와 기록 읽기
  오류는 로케일이 정해지기 *전에* 던져질 수 있다. 생성 시 영어로 `.message`를
  채워 스택 트레이스를 살리고, 진입점 한 곳이 `.key`를 활성 로케일로 다시
  렌더한다.
- **섹션·그룹을 표시 문자열에서 id로 갈랐다.** `SECTION_ORDER`가 정렬 키이자 탭
  이름이자 상태의 원소였으므로 번역하면 정렬이 깨졌다. 검색어에는 id·영어
  라벨·현재 언어 라벨을 함께 넣어 한국어 화면에서도 `plugin`으로 찾힌다.
- **런처 오류 문구만 영·한 병기로 예외를 두었다.** Node가 없을 때 찍히는 줄이라
  i18n 기계장치가 아예 돌지 못한다. `setup-agents.ps1`에는 `-Lang`을 더했다 —
  `.sh`는 `"$@"`로 저절로 통하지만 `.ps1`은 명명 파라미터 방식이라 `--lang`이
  닿지 않았다.

주석과 커밋 메시지는 한국어를 유지한다. 유지보수자용이고 `.gitmessage.txt`가
한국어 본문을 요구한다. 76개 `DESIGN.md` 번들 본문도 공급자 원본이라 번역
대상이 아니다.

### 번역 누락을 검사로 못 박다

방향이 둘이다. 영어 로케일로 CLI 표면 전부를 돌려 출력에 한글이 하나도 없음을
단언하고(`i18n.en.test.mjs`), **그 반대** — 한국어 출력에 영어 문장이 남는 것 —
도 본다(`i18n.ko.test.mjs`). 뒤쪽을 아무도 보지 않는 동안 `writeRecord`의 영어
폴백이 한국어 출력 한가운데에서 한 줄을 영어로 찍고 있었다.

카탈로그 자체도 데이터로 검사한다. en·ko의 키 집합·`{자리}` 집합·값 타입이
같은지, 미지 키나 빠진 자리 값에 `t()`가 던지는지, 두 로케일 모두에서 행 라벨의
표시 폭이 `LABEL_WIDTH` 이하인지. 폭 상수는 베끼지 않고 `render.mjs`에서
import 한다 — 두 값이 따로 놀면 규칙이 조용히 무의미해진다.

`1.2.0`은 124 파일로 발행된다.

## 설치 기록과 갱신 (2026-07-30, 1.1.0)

**한 번 생성된 파일이 절대 갱신되지 않던 상태를 고쳤다.** `manifest.mjs`의
`files`는 "없을 때만 생성"이고 `blocks`는 마커가 없을 때만 덧붙였으므로,
템플릿이 개선되어도 기존 저장소에 전달될 길이 없었다.

- **설치 기록 `.agent-kit/agent-setup.json`.** 커밋 대상이며 배선 버전,
  고른 항목, 관리 파일별 해시를 담는다. 문서화된 "상태 파일이 없습니다"
  원칙이 둘로 갈렸다 — **스캔은 실제, 기록은 의도**다. 기록은 판정을
  대체하지 않고 재현성과 버전 고정을 더한다.
- **`update`.** 기록된 해시와 일치하는 파일만 최신 템플릿으로 옮기고,
  사용자가 고친 파일은 건드리지 않고 드리프트로 보고한다. 파일이 없으면
  생성하므로 새 도구 지원이 이 경로로 전달된다. `--force`는 드리프트까지
  덮어쓰지만 **워킹트리가 깨끗할 때만** 허용한다 — git이 유일한 되돌리기
  수단이다.
- **`status`.** 의도·실제·버전을 나란히 보여주고 `--json`으로 CI에서 쓸 수
  있다. 갱신 판정은 `update`와 같은 함수를 dry-run으로 불러 얻으므로 두
  명령이 다른 답을 낼 수 없다.
- **`bootstrap --adopt`.** 설치기를 복사해 쓰던 저장소를 기록 체계로
  끌어온다. 파일을 만들지 않고, **템플릿과 같은 파일만** 채택한다. 현재
  내용을 그대로 해시로 박으면 이미 고쳐 둔 파일이 "우리가 쓴 그대로"로
  위장되어 다음 `update`에 날아간다.
- **해시는 줄바꿈을 LF로 정규화한 뒤 계산한다.** `.gitattributes`가
  `text=auto`라 Windows 워킹트리는 CRLF다. 원시 바이트를 해시하면 그
  환경에서 모든 파일이 드리프트로 뜬다.
- **블록은 마커 사이만 교체한다.** `CLAUDE.md`·`GEMINI.md`에 사용자가 쓴
  주변 내용은 그대로 남는다.

기존 명령(`bootstrap`, `--list`, `--set`, `design`)은 그대로 동작한다.
버전 불일치는 보고만 하며, 중단시키는 엄격 검사는 명령 표면을 재편하는
다음 단계로 미뤘다 — 여기서 중단하면 기존 사용자의 실행이 죽는다.

### 발행물에서 이물질 제거

`1.1.0` 발행 로그에서 `lib/items/.omc/state/sessions/…/pre-tool-advisory-throttle.json`
이 tarball에 실려 있는 것을 발견해 제거했다. 로컬 도구가 만든 세션 상태
파일이며 `1.0.0`에도 들어갔다(116 파일 중 1개). 내용은 훅 스로틀 타임스탬프와
조언 문구뿐이라 비밀값은 없다.

원인은 **`package.json`의 `files`가 `.gitignore`보다 우선**한다는 것이다.
`.gitignore:435`의 `.omc/`가 git에서는 정상 작동해 추적되지 않았지만,
`files`에 `lib/`를 나열했으므로 npm은 그 아래 전부를 무조건 담았다.
`.npmignore`로도 막을 수 없다.

pack 검증 테스트가 최상위 경로만 보고 있어 `lib/` 하위에 낀 것을 놓쳤다.
두 가지를 더했다.

- 경로의 **어느 구간이든 `.`으로 시작하면** 실패
- `.mjs`·`.json`·`.md`와 `LICENSE` 외의 확장자가 있으면 실패

`1.1.0`은 119 파일로 발행된다.

## npm 발행 (2026-07-30)

파일을 복사하지 않고 쓸 수 있게 됐다. 설치기를 `@rch4com/agent-setup`으로
npm에 발행한다.

이름에 스코프가 붙은 이유는 **스코프 없는 `agent-setup`을 쓸 수 없어서**다.
npm은 유사 이름을 차단하고, `agent-setup`은 기존 패키지 `agentsetup`과
정규화하면 같아져 발행이 403으로 거부된다. `npm view agent-setup`이 404를
돌려주는 것만으로는 발행 가능을 확인할 수 없다 — 이 제한은 발행 시점에만
걸린다. `bin` 이름은 `agent-setup`으로 두었으므로 설치 후 실행 명령은 짧다.

- **`npx @rch4com/agent-setup bootstrap`으로 배선.** 기존 명령(`bootstrap`,
  `--list`, `--set`, `design`)이 그대로 동작한다. 이 단계에서 CLI 표면은
  바뀌지 않았다.
- **팀 저장소 커밋 대상에서 벤더링이 빠졌다.** `agent-installer/`와 런처 2개는
  선택 사항이 됐다. 오프라인 환경이나 커밋된 진입점이 필요하면 기존처럼
  함께 커밋하는 방식도 그대로 동작한다.
- **tarball 내용물을 테스트가 지킨다.** 발행은 되돌릴 수 없으므로 최상위 경로
  화이트리스트, `test/`·`scripts/` 누출 금지, DESIGN.md 번들 존재, 2MiB
  상한을 `npm pack --dry-run --json`으로 검사한다. 실측 tarball은 0.58MB라
  76개 DESIGN.md 번들을 그대로 동봉한다.
- **MIT 라이선스 추가.** `files`가 패키지 디렉터리 밖에 닿을 수 없어 루트와
  `agent-installer/`에 사본을 두고, 두 파일이 갈라지지 않도록 동일성을
  테스트로 묶었다.

## 2차 점검 반영 (2026-07-30)

앞선 점검에서 남았던 항목을 반영. 동작이 바뀐 것은 아래 넷.

- **MCP·플러그인 쓰기 경로의 링크 이탈 검사.** 부트스트랩·design.md·gstack만
  realpath 검사를 하고 있었고, 정작 가장 넓은 쓰기 경로인 10개 CLI의 MCP
  등록·제거와 `.claude/settings.json` 플러그인 기록이 어휘적 검사에 머물러
  있었다. 저장소 안 `.codex`·`.claude`가 홈을 가리키는 Junction이면 항목
  하나를 고르는 것으로 글로벌 설정이 생성·수정됐다.
- **Windows 셸 인용.** 공백이 있을 때만 감싸던 것을 항상 감싸도록 바꿨다.
  `D:\R&D\repo` 아래에서는 gstack clone 명령이 `&`에서 둘로 쪼개졌다.
  POSIX 쪽도 작은따옴표로 바꿔 `$(...)`·백틱까지 막는다.
- **원격 응답 크기 상한(8MiB).** 시간 제한은 "데이터가 계속 오는" 응답을
  막지 못해, 끝나지 않는 본문을 메모리가 다 찰 때까지 읽었다. 번들 재생성
  스크립트도 맨 `fetch`에서 `netFetch`로 옮겨 두 제한을 함께 받는다.
- **상호 배타 플래그 거부.** `--list --set a`는 `--set`을, `design --preview x
  --set y`는 `--set`을 조용히 버렸다. 모르는 인자를 거부하는 것과 같은 이유로
  거부한다.

그 밖에 화면에 나가는 원격 텍스트에서 bidi 오버라이드·폭 없는 유니코드
서식문자를 함께 걷어내고, 미사용 의존성 `@clack/prompts`를 지웠으며(TUI는 Node
표준 라이브러리만 쓴다), 문서에 없던 `.kilocode/mcp.json`과 번들 개수(76)를
바로잡았다.

## 코드·문서 점검 반영 (2026-07-29)

전체 점검에서 나온 항목을 반영. 동작이 바뀐 것은 아래 넷.

- **미리보기 명령 주입 차단.** Windows 오프너가 `cmd /c start`를 써서,
  원격 README에서 온 이름에 `&`가 섞이면 그대로 명령이 실행됐다. webUrl
  인코딩 + 카탈로그 이름 검증 + 셸을 거치지 않는 rundll32로 세 겹을 막고,
  http(s) URL이나 실제로 존재하는 경로만 열도록 제한.
- **`.vscode/settings.json` 부정 항목 추가.** `chat.useAgentsMdFile`이 사는
  파일인데 `.vscode/*`를 무시하는 저장소에서 커밋되지 않아 팀에 전파되지
  않았다.
- **인자 처리.** 루트·design 파서가 모르는 인자를 무시하던 것을 거부로
  바꾸고, 값 플래그가 `--flag=값` 형식도 받도록 통일. 루트에 `--help`를
  붙이고, 버려지던 `--skill-mode`를 대화형 화면까지 전달.
- **설치기 쓰기 경로의 링크 이탈 검사.** design.md 설치·제거, gstack
  clone·삭제, `.gitignore` 쓰기가 어휘적 검사만 하고 있었다.

그 밖에 dry-run이 MCP·design.md 변경을 보고하도록 하고, 네트워크 호출에
20초 제한을 걸었으며, 원격 텍스트의 제어문자를 화면에 내보내지 않도록 했다.
문서는 `--tui` 명칭, Copilot의 stdio MCP 형식 차이, 설치기가 실행하는
제3자 명령을 바로잡거나 새로 적었다.

## GitHub Copilot 지원 (2026-07-29)

- GitHub Copilot CLI와 VS Code Copilot을 지원 도구에 추가. 두 도구 모두
  루트 `AGENTS.md`와 `.agents/skills`를 네이티브로 읽어 import 배선과
  스킬 어댑터가 필요 없음.
- 프로젝트 MCP는 Copilot CLI가 `.github/mcp.json`(`mcpServers`, 로컬 서버는
  `type: "local"`), VS Code가 `.vscode/mcp.json`(`servers`, 로컬 서버는
  `type: "stdio"`). 설치기의 MCP 등록 대상이 8개에서 10개로 늘어남.
- 팀 공유 설정 자리로 `.github/copilot/settings.json`을 만들고, 개인
  오버라이드 `.github/copilot/settings.local.json`은 `.gitignore` 처리.
- VS Code는 `chat.useAgentsMdFile` 설정이 있어야 `AGENTS.md`를 읽으므로
  `.vscode/settings.json`에 키가 없을 때만 추가(기존 값은 보존).
  이를 위해 부트스트랩에 `ensureJsonKeys` 실행기를 추가 — 외부 의존성
  없이 텍스트 삽입으로 처리해 주석·포맷을 보존함.
- `VisualStudio.gitignore`가 `.vscode/*`를 무시하므로 `!.vscode/mcp.json`
  부정 항목을 함께 추가.
- 클라우드 코딩 에이전트는 범위 밖 — 지침은 `AGENTS.md`로 이미 커버되고,
  MCP는 저장소 파일이 아니라 GitHub 웹 설정에서 구성됨.

## Antigravity 지원 (2026-07-18)

- Antigravity(Google 에이전트 IDE/CLI)를 지원 도구에 추가.
  루트 `AGENTS.md`와 `.agents/` 디렉터리를 네이티브로 인식하므로
  기존 공유 `AGENTS.md`·`.agents/skills`를 그대로 사용 —
  import 배선·어댑터·신규 파일이 필요 없음.
- MCP는 홈 글로벌(`~/.gemini/config/mcp_config.json`)에서만 설정되고
  프로젝트 스코프 MCP 파일이 없어 스크립트 관리 범위 밖 →
  agent-installer의 프로젝트 MCP 등록 대상에서도 제외.
- 생성 문서 문구와 완료 요약의 도구 목록에만 반영(구조 변화 없음).
- 프로젝트 루트 `.mcp.json` 적용 요청은 반려 — Google 공식 포럼 확인 결과
  Antigravity는 프로젝트 스코프 MCP를 미지원(기능 요청 상태)이며 루트
  `.mcp.json`을 읽지 않음(해당 주장은 비공식 brain 노트의 오류).

## Matt Pocock skills 플러그인 항목 (2026-07-17)

- agent-installer에 `plugin.mattpocock-skills` 추가 — Claude Code
  마켓플레이스 `mattpocock/skills`에서 `mattpocock-skills@mattpocock`
  플러그인(엔지니어링·생산성 스킬 22종)을 프로젝트 범위로 설치.

## Grok Build 지원 (2026-07-17)

- Grok Build(xAI grok CLI)를 지원 도구에 추가.
  루트 `AGENTS.md`를 네이티브로 읽으므로 import 배선이 필요 없음.
- 프로젝트 설정 `.grok/config.toml` 생성
  (프로젝트 스코프는 mcp_servers·plugins·permission rules만).
- `.grok/skills`를 `.agents/skills`의 Junction/링크/복제본으로 구성하고
  `.gitignore`에 자동 추가.
- agent-installer에 grok MCP 어댑터 추가 — MCP 항목이 8개 CLI
  프로젝트 설정에 동시 등록됨.
- 공식 MiniMax CLI(`mmx`)는 코딩 에이전트가 아니어서(프로젝트 규약
  없음) 지원 대상에서 제외. 스킬 설치 항목도 검토 후 롤백함.

## agent-installer 추가 (2026-07-17)

- 플러그인·MCP·스킬을 체크박스로 골라 설치/제거하는 자기완결 콘솔
  도구 `agent-installer/` 추가. 상태 파일 없이 실제 설정 파일을
  스캔해 판정하며 `--list`, `--dry-run`, `--set` 비대화형 모드 지원.

## Kimi Code 지원

- 루트 `AGENTS.md`와 `.agents/skills`를 네이티브로 사용.
- 프로젝트 MCP 파일 `.kimi-code/mcp.json` 생성.
- 머신별 설정 `.kimi-code/local.toml`을 `.gitignore`에 추가.

## Kilo Code / Kiro 지원

- 프로젝트 로컬 `kilo.jsonc`와 `.kiro/settings/mcp.json` 생성.
- `.kiro/skills`를 `.agents/skills`의 Junction/링크/복제본으로 구성.
- 사용자 홈이나 전역 에이전트 설정은 쓰지 않음.
